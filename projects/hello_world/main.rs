fn main() {
    //El signo de admiración luego de la llamada
    // indica que es un macro y no una función.
    println!("Hello, world!");
}

// usar `rustc main.rs` para compilar el programa
// en windows llamar a `main.exe` en linux a `main`